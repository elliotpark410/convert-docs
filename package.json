{
  "name": "poas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node ./index.js",
    "docker-build": "docker build . -t garuda-docs-image",
    "docker-run": "docker run --name garuda-docs-container -it --rm -dp 4444:80 garuda-docs-image",
    "open": "start http://localhost:4444/",
    "docker-remove": "docker rm -f garuda-docs-container",
    "convert": "npm run start && npm run docker-build && npm run docker-run && npm run open && npm run docker-remove"
    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "postman-to-openapi": "^2.4.0"
  }
}
